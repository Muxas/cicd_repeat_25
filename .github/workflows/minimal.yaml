on:
    push
jobs:
    idle:
        runs-on: ubuntu-latest
        steps:
            -
                run: |
                    echo Idle Step
                    sudo apt update
                    sudo apt install figlet
